using UnityEngine;

public class TestDebug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Je suis sur la branch master");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
